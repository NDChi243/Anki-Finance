# -*- coding: utf-8 -*-
from __future__ import annotations
"""
rank_system.py — Hệ thống cấp bậc Anki Tycoon.

Điều kiện lên rank: ĐẠT ĐỦ CẢ BA:
   1. XP tích lũy >= xp_required
   2. Balance hiện tại >= balance_required
   3. Điểm Kiến Thức (KN) >= kn_required (từ Tiểu thương trở lên)

Cấu trúc rank: tên_nhóm + cấp (1,2,3...)

Lưu ý: Người chơi được cấp 10M VND + nhà trọ ban đầu,
      nên yêu cầu balance được thiết kế cao dần từ đó.
"""

from ._safe_config import col_ready, cfg_dict, cfg_set, cfg_int

_KEY_XP   = "anki_tycoon_xp"
_KEY_KN   = "anki_tycoon_knowledge"
_KEY_RANK = "anki_tycoon_rank"

# ── Bảng rank ─────────────────────────────────────────────────────
# (id, label, xp_required, balance_required, kn_required, emoji, color_hex)
RANKS = [
    # ── Sinh viên (cấp 1-3) ──
    {"id":"sv1","label":"Sinh viên cấp 1","xp":0,        "bal":0,          "kn":0,   "emoji":"📚","color":"#94a3b8","group":"Sinh viên"},
    {"id":"sv2","label":"Sinh viên cấp 2","xp":200,      "bal":10_000_000, "kn":0,   "emoji":"📖","color":"#94a3b8","group":"Sinh viên"},
    {"id":"sv3","label":"Sinh viên cấp 3","xp":500,      "bal":20_000_000, "kn":0,   "emoji":"🎓","color":"#94a3b8","group":"Sinh viên"},

    # ── Người làm công (cấp 1-3) ──
    {"id":"nlc1","label":"Người làm công cấp 1","xp":1_000,  "bal":50_000_000,   "kn":0,   "emoji":"👷","color":"#f59e0b","group":"Người làm công"},
    {"id":"nlc2","label":"Người làm công cấp 2","xp":2_500,  "bal":100_000_000,  "kn":0,   "emoji":"🧑‍💼","color":"#f59e0b","group":"Người làm công"},
    {"id":"nlc3","label":"Người làm công cấp 3","xp":5_000,  "bal":250_000_000,  "kn":0,   "emoji":"💼","color":"#f59e0b","group":"Người làm công"},

    # ── Tiểu thương (cấp 1-3) ──
    {"id":"tt1","label":"Tiểu thương cấp 1","xp":10_000, "bal":500_000_000,   "kn":1_000,  "emoji":"🏪","color":"#10b981","group":"Tiểu thương"},
    {"id":"tt2","label":"Tiểu thương cấp 2","xp":20_000, "bal":1_000_000_000, "kn":2_500,  "emoji":"🏬","color":"#10b981","group":"Tiểu thương"},
    {"id":"tt3","label":"Tiểu thương cấp 3","xp":35_000, "bal":2_000_000_000, "kn":5_000,  "emoji":"🛒","color":"#10b981","group":"Tiểu thương"},

    # ── Doanh nhân (cấp 1-4) ──
    {"id":"dn1","label":"Doanh nhân cấp 1","xp":50_000,  "bal":5_000_000_000,    "kn":10_000,   "emoji":"🤵","color":"#3b82f6","group":"Doanh nhân"},
    {"id":"dn2","label":"Doanh nhân cấp 2","xp":80_000,  "bal":10_000_000_000,   "kn":20_000,   "emoji":"🏢","color":"#3b82f6","group":"Doanh nhân"},
    {"id":"dn3","label":"Doanh nhân cấp 3","xp":120_000, "bal":20_000_000_000,   "kn":40_000,   "emoji":"✈️","color":"#3b82f6","group":"Doanh nhân"},
    {"id":"dn4","label":"Doanh nhân cấp 4","xp":170_000, "bal":50_000_000_000,   "kn":70_000,   "emoji":"🌐","color":"#3b82f6","group":"Doanh nhân"},

    # ── Triệu phú (cấp 1-3) ──
    {"id":"tp1","label":"Triệu phú cấp 1","xp":250_000,  "bal":100_000_000_000,   "kn":120_000,   "emoji":"💎","color":"#a855f7","group":"Triệu phú"},
    {"id":"tp2","label":"Triệu phú cấp 2","xp":400_000,  "bal":200_000_000_000,   "kn":200_000,   "emoji":"👑","color":"#a855f7","group":"Triệu phú"},
    {"id":"tp3","label":"Triệu phú cấp 3","xp":600_000,  "bal":500_000_000_000,   "kn":350_000,   "emoji":"🏆","color":"#a855f7","group":"Triệu phú"},

    # ── Tỷ phú (cấp 1-3) ──
    {"id":"typh1","label":"Tỷ phú cấp 1","xp":900_000,   "bal":1_000_000_000_000,   "kn":600_000,   "emoji":"🚀","color":"#ec4899","group":"Tỷ phú"},
    {"id":"typh2","label":"Tỷ phú cấp 2","xp":1_500_000, "bal":2_000_000_000_000,   "kn":1_000_000, "emoji":"🌙","color":"#ec4899","group":"Tỷ phú"},
    {"id":"typh3","label":"Tỷ phú cấp 3","xp":2_500_000, "bal":5_000_000_000_000,   "kn":1_800_000, "emoji":"⭐","color":"#fbbf24","group":"Tỷ phú"},

    # ── Thạc sĩ Tài chính (cấp 1-3) — MỚI ──
    {"id":"mstc1","label":"Thạc sĩ Tài chính cấp 1","xp":4_000_000,  "bal":10_000_000_000_000,  "kn":3_000_000,  "emoji":"💼","color":"#f43f5e","group":"Thạc sĩ Tài chính"},
    {"id":"mstc2","label":"Thạc sĩ Tài chính cấp 2","xp":6_000_000,  "bal":20_000_000_000_000,  "kn":5_000_000,  "emoji":"🎯","color":"#f43f5e","group":"Thạc sĩ Tài chính"},
    {"id":"mstc3","label":"Thạc sĩ Tài chính cấp 3","xp":10_000_000, "bal":50_000_000_000_000,  "kn":8_000_000,  "emoji":"💎","color":"#f43f5e","group":"Thạc sĩ Tài chính"},

    # ── Huyền thoại (max rank) — MỚI ──
    {"id":"hl1","label":"Huyền thoại Anki","xp":20_000_000, "bal":100_000_000_000_000, "kn":15_000_000, "emoji":"👑","color":"#fbbf24","group":"Huyền thoại"},
]

# XP per ease (base, trước boost)
XP_PER_EASE = {1: 2, 2: 8, 3: 15, 4: 25}


# ── XP API ────────────────────────────────────────────────────────

def get_xp() -> int:
    return cfg_int(_KEY_XP, 0)

def add_xp(amount: int) -> None:
    if not col_ready(): return
    cfg_set(_KEY_XP, get_xp() + int(amount))

def get_xp_for_ease(ease: int) -> int:
    return XP_PER_EASE.get(ease, 0)


# ── KN (Kiến Thức) API ───────────────────────────────────────────

def get_kn() -> int:
    """Lấy điểm Kiến Thức hiện tại."""
    return cfg_int(_KEY_KN, 0)

def add_kn(amount: int) -> None:
    """Cộng điểm Kiến Thức."""
    if not col_ready(): return
    cfg_set(_KEY_KN, get_kn() + int(amount))


# ── Rank logic ────────────────────────────────────────────────────

def _calc_rank(xp: int, balance: int, kn: int = 0) -> dict:
    """Tính rank hiện tại dựa trên xp + balance + kn."""
    current = RANKS[0]
    for r in RANKS:
        kn_req = r.get("kn", 0)
        if xp >= r["xp"] and balance >= r["bal"] and kn >= kn_req:
            current = r
        else:
            break
    return current

def _next_rank(current_id: str) -> dict | None:
    ids = [r["id"] for r in RANKS]
    if current_id not in ids: return None
    idx = ids.index(current_id)
    return RANKS[idx + 1] if idx + 1 < len(RANKS) else None


def get_rank_status(balance: int | None = None) -> dict:
    """
    Trả về toàn bộ thông tin rank hiện tại + progress lên rank tiếp theo.
    Bao gồm cả điểm Kiến Thức (KN).
    """
    if not col_ready():
        return _empty_status()

    from .balance import get_balance as _get_bal
    if balance is None:
        balance = _get_bal()

    xp      = get_xp()
    kn      = get_kn()
    current = _calc_rank(xp, balance, kn)
    nxt     = _next_rank(current["id"])

    # Progress tới rank tiếp theo
    xp_pct  = 0.0
    bal_pct = 0.0
    kn_pct  = 0.0
    if nxt:
        xp_pct  = min(100.0, xp  / nxt["xp"]  * 100) if nxt["xp"]  > 0 else 100.0
        bal_pct = min(100.0, balance / nxt["bal"] * 100) if nxt["bal"] > 0 else 100.0
        kn_req  = nxt.get("kn", 0)
        kn_pct  = min(100.0, kn / kn_req * 100) if kn_req > 0 else 100.0
        if kn_req > 0:
            overall_pct = min(xp_pct, bal_pct, kn_pct)   # phải đủ CẢ BA
        else:
            overall_pct = min(xp_pct, bal_pct)            # phải đủ CẢ HAI
    else:
        kn_pct = 100.0
        overall_pct = 100.0

    return {
        "xp":           xp,
        "kn":           kn,
        "rank_id":      current["id"],
        "rank_label":   current["label"],
        "rank_emoji":   current["emoji"],
        "rank_color":   current["color"],
        "rank_group":   current["group"],
        "next_rank":    nxt,
        "xp_pct":       round(xp_pct, 1),
        "bal_pct":      round(bal_pct, 1),
        "kn_pct":       round(kn_pct, 1),
        "overall_pct":  round(overall_pct, 1),
        "xp_needed":    max(0, nxt["xp"] - xp) if nxt else 0,
        "bal_needed":   max(0, nxt["bal"] - balance) if nxt else 0,
        "kn_needed":    max(0, nxt.get("kn", 0) - kn) if nxt else 0,
        "is_max":       nxt is None,
    }

def _empty_status() -> dict:
    r = RANKS[0]
    return {"xp":0,"kn":0,"rank_id":r["id"],"rank_label":r["label"],
            "rank_emoji":r["emoji"],"rank_color":r["color"],
            "rank_group":r["group"],"next_rank":RANKS[1],
            "xp_pct":0,"bal_pct":0,"kn_pct":0,"overall_pct":0,
            "xp_needed":RANKS[1]["xp"],"bal_needed":RANKS[1]["bal"],"kn_needed":RANKS[1].get("kn",0),"is_max":False}

def get_all_ranks() -> list:
    return RANKS